To Do:
	test git
	thread safety
	JTable row names
	add interupts
	thread pool
	use locks in controller
	overflow check method
	extend check method
	proper stateful notifications
	change abst class to interface

Implemented instuctions:
	MOVE (partial)
	MOVEQ (completes
